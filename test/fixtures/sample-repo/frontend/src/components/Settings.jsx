import React from 'react';

// Settings page: IMAP account form.
const Settings = () => {
  return (
    <form>
      <label>IMAP host <input name="host" /></label>
    </form>
  );
};

export default Settings;
